# Changelog

## 1.4.0 · r26 cognitive live agent

- Холодный запуск голоса теперь выполняет реальную пробную ASR-инференцию и прогрев TTS при старте; ранняя фраза владельца остаётся в очереди, а не теряется.
- `Отмена`, `стоп`, `отмени задачу` и распространённый ASR-вариант `первая отмена` работают без wake-word и останавливают озвучку, чат, GUI-миссию и фоновые задачи одним контуром.
- На CPU выбран быстрый локальный Piper-lane вместо многосекундного Silero-lane; частые служебные ответы кэшируются после синтеза.
- Добавлен privacy-bounded журнал опыта: после четырёх неудачных подходов меняется стратегия, а подтверждённые маршруты могут сохраняться как локальные навыки.
- Перед изменением файла создаётся ограниченная контрольная точка; команда `откати последнее` восстанавливает предыдущую версию.
- Долговременная память различает факты, предпочтения, привычки, проекты, отношения и решения; поддержано адресное забывание.
- Эмоциональный фон кратковременно сохраняется между репликами и влияет на речь, формулировки и глаза desktop companion.
- Telegram понимает несколько получателей в одной фразе, корректно обрабатывает Избранное, не выбирает канал по совпадению внутри описания и отличает дату `Aug 9` от unread-бейджа.
- Добавлено выключенное по умолчанию управление с телефона через Telegram: только точный allowlist, обязательный префикс и локальная Telethon-session.
- Проактивность получила privacy-gate, предложения сохранить повторяемый маршрут как навык и более строгую паузу на звонках, оплате, CAPTCHA/2FA и демонстрации экрана.
- Добавлены 22 unit-теста, включая cold-start probe, голосовую отмену, Telegram parsing/unread, rollback, skill learning и stop-generation.

## 1.3.0 · r25 adaptive live agent

- Общий `AdaptiveRecovery`: после четырёх неудачных подходов агент меняет стратегию и передаёт следующему планировщику журнал уже проверенных гипотез.
- Неопределённый commit никогда не повторяется автоматически; область удаления «у меня/у всех» сверяется с видимой кнопкой подтверждения.
- Telegram collection-loop прокручивает именно левую панель чатов до стабильного конца, запоминает обработанные строки и продолжает после каждого ответа.
- Добавлен детерминированный кросс-приложенческий граф МЭШ → домашнее задание → Избранное Telegram.
- Голос получил расширенные эмоциональные режимы, анализ темпа/энергии/просодии и синхронизацию с глазами desktop companion.
- Проактивный наблюдатель формирует редкие, конкретные реплики по UIA-контексту, не архивирует экран и отсекает чувствительные поверхности.
- Добавлены unit-тесты resilience, affect, Telegram scrolling, уточнений и эмоциональных глаз; тесты включаются в релизный ZIP.

## 1.2.2 · r24 full-agent hotfix

- Установщик больше не падает на 98% из-за необязательной one-file PyInstaller-сборки: проверенное `.venv` считается рабочим runtime, а EXE имеет безопасный launcher fallback.
- `logs/install.log` теперь реально сохраняет ход установки, а окно ошибки показывает последние значимые строки вместо случайного начала вывода PyInstaller.
- Windows-сборка использует закреплённые build-зависимости и проверяет наличие PE-иконки без хрупкого попиксельного сравнения разных ICO-кадров.
- В открытом VS Code фраза вроде «Эрви, где баг?» запускает фоновую проверку реальной workspace: чтение файлов, воспроизведение ошибки, минимальный фикс и повторные тесты/сборку.
- Естественные жалобы вроде «у меня не работает Яндекс Музыка, почему?» запускают реальную диагностику; добавлена явная безопасная переустановка одного приложения через winget.
- Миссия «МЭШ → домашнее задание → Избранное Telegram» передаёт между приложениями типизированный текстовый артефакт, а не местоимение «его».
- Проактивный наблюдатель отслеживает смену активного окна, замечает зависшие приложения и видимые ошибки VS Code, а лимит отдыха по видео по-прежнему умеет ставить медиа на паузу.

## 1.2.2 · r23 agent reliability

- Все экранные контуры используют одну блокировку рабочего стола: старая задача больше не кликает поверх новой команды.
- Длинные цепочки детерминированно разделяются на последовательные медиа-, веб-, UI- и Telegram-этапы без ожидания модели для самого плана.
- «Открой/включи» больше не подменяют смысл на «сайт/приложение»: явный контекст учитывается, а неоднозначный маршрут уточняется.
- Telegram корректно понимает «Избранное», не принимает слова «сообщение/текст» за получателя и умеет брать выделенный в Проводнике файл.
- Windows-сборка создаёт `EIRVEN-AI-r23.exe`, сверяет встроенную PE-иконку с новым ICO и обновляет кэш оболочки.

## 1.2.2 · r22.4 hotfix

- Telegram Web подтверждает ввод по реальному переходу кнопки композера из `record` в `send`, даже когда Chromium скрывает значение `contenteditable` от UIA.
- Сообщение отправляется одним семантическим кликом по `btn-send`; после успешного клика Enter больше не нажимается.
- Windows-сборка удаляет старый EXE перед PyInstaller, использует абсолютный путь к новому ICO, добавляет обновлённый version resource и проверяет наличие встроенной иконки.
- Favicon заменён отдельным PNG с прозрачным фоном из нового логотипа EIRVEN.

## 1.2.2 · r22.3 hotfix

- Исправлен ввод сообщений в браузерных `contenteditable`-полях Telegram: фокус ставится в реальную зону каретки, а вставка имеет безопасный идемпотентный резервный путь.
- Голосовая активация и все подсказки теперь используют только имя ассистента из настроек; старое имя не остаётся скрытым wake-словом.
- Расширена последняя проверка женского рода для ответов чата и фоновых задач.
- Все PNG/ICO-ресурсы, окно запуска, EXE, ярлык рабочего стола и ярлык автозапуска используют новый логотип.

## 1.2.2 — Living Desktop & Resilient Installer

- Установщик автоматически повторяет упавшие команды и до трёх раз перезапускает bootstrap без ручного закрытия окна. Уже скачанные модели и окружение сохраняются.
- На экране установки используется живая высокодетализированная EIRVEN-сфера и официальный логотип.
- Официальный логотип EIRVEN используется в web UI, desktop companion, Windows icon, ярлыке и installer.
- Desktop companion получил выразительные глаза, которые реагируют на listening / thinking / speaking / success.
- Комментарии desktop-сферы переработаны в короткие человеческие фразы и liquid-glass карточку.
- В `Настройки → Внешний вид` добавлен переключатель живых глаз.
- В `Настройки → Общее` добавлена безопасная кнопка `Отключить EIRVEN`.
- Версия обновлена до 1.2.2; GitHub Release pipeline и update checker публикуют/ищут новый Windows ZIP.

## 1.2.1 — Public Release

- Финальный r22 reliability router и единый ownership команд.
- Живая liquid-glass сфера с премиальным `E I R V E N` wordmark.
- Единственная публичная identity: женская Эйрвен, фирменный голос Бая.
- Мужские voice-профили и их загрузка удалены из публичного продукта.
- Настройки: Общее / Голос / Внешний вид / Приватность / Обновления.
- GitHub Releases update checker с stable/preview каналами и release asset link.
- Упрощён чистый Windows installer и автозапуск.
- Репозиторий очищен от runtime DB, логов, старых patch/migration artifacts и caches.
- Полный набор публичной документации и demo-script.

## 1.2.0

Внутренняя серия r16–r22: autonomous workflow, grounding, browser recovery, long-horizon missions, hardening, living presence и final reliability.

### 1.2.2 final reliability refresh

- restored the r22 action core as the behavioral baseline;
- fixed Yandex Music Play recognition from visible UIA;
- fixed repeated wake-word stripping and tolerant Telegram Web search verification;
- fixed real process shutdown through the supervisor;
- replaced desktop eyes with two soft line-only expressions;
- uses the transparent EIRVEN orb artwork throughout the public UI.
