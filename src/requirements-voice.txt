onnx-asr[cpu,hub]>=0.12,<1.0
faster-whisper>=1.2,<2.0
soundfile>=0.13,<1.0
sounddevice>=0.5,<1.0
webrtcvad-wheels>=2.0.14,<3.0
# Pinned below 2.4: opencv-python is pinned to a January-2025 build and the
# onnxruntime/ctranslate2 wheels are compiled against the NumPy 2.0-2.3 ABI.
# Letting pip resolve a brand-new NumPy (2.5.x) pairs a fresh core with older
# compiled extensions and surfaces as an opaque
# "DLL load failed while importing _multiarray_umath" during first install.
numpy>=1.26,<2.4
edge-tts>=7.2,<8.0
# Silero RU model is downloaded and validated by bootstrap. Expressive TTS is optional and
# intentionally not installed by default.

# Optional natural multilingual neural voice; bootstrap installs v0.1.7 opportunistically on CUDA.
# chatterbox-tts==0.1.7
# Edge neural speech is the primary natural voice when online; the packaged Silero model
# remains an explicit offline degraded fallback.
